version: "3"
services:
  api:
    container_name: api
    restart: always
    build: ../frontend
    ports:
      - "8080:8080"
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./.mongo_volume:/data/db
    ports:
      - "27017:27019"